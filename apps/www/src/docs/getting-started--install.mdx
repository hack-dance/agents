
# Installation


## Existing projects
Install the necessary dependencies with your package manager of choice.
  ```bash
    pnpm add openai zod
  ```

Add the core agent utilities
  ```zsh
    npx hack-dance add agents core
  ```


Optionally setup import aliases
  ```json
    {
      "compilerOptions": {
        "paths": {
          "@/*": ["./*"]
        }
      }
    }
  ```

  {/* ## New project
  We have built a few templates to get you started quickly.

  - Next.js Chatbot
  - Next.js Rich UI */}
